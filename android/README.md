# android (placeholder)

The Veilens Android client — a Kotlin/Jetpack Compose app: chat plus the
workflow/approval/debug panel. A thin client of the [protocol](../protocol),
reaching the local `server/` over Tailscale.

Not scaffolded yet. Planned: a Compose app whose protocol client is generated
from `../protocol`.
